package com.martinfou.trading.tui;

/** Default backtest sizing shown in the interactive wizard (matches control-plane defaults). */
final class TuiDefaults {

    static final double STARTING_CAPITAL = 1_000.0;
    static final double LOT_SIZE = 0.01;

    private TuiDefaults() {}
}
